export const reStateAction = () => ({
    type: 'RE_STATE'
});

export const chooseClass = (key) => ({
    type: 'CHOOSECLASS',
    key
});

export const deleteClass = (key) => ({
    type: 'DELETECLASS',
    key
});

